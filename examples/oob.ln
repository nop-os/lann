let foo mem_alloc(16)
let foo mem_realloc(foo + 753648, 753632)

printf("[]\n", to_type(foo, number))

let count 0

while count < 753632: begin
  let byte mem_read(foo + count)
  if byte >= 32 and byte != 127 put_char(byte)
  
  let count count + 1
end
